function captureEntireScreen() {
    // let full_ctx = container.getContext('2d');
    //
    // let shot = document.createElement("canvas");
    // shot.setAttribute("id", "shot-1");
    // document.body.appendChild(shot);
    //
    // console.log(shot);
    // shot.getContext("2d").drawImage(full_ctx, 10, 10);
    //
    // shot.style.top = '0';
    // shot.style.left = '0';
    // shot.style.position = 'absolute';
    // pinLayer.appendChild(shot);
}

/**
 * @param {MouseEvent} mouseEvent
 */
function takeScreenShot(mouseEvent) {
    // gray overlay
    // select area
    captureEntireScreen();
    // crop
    // show
}
